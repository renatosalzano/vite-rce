const CONFIG_ID = '$';
const HOOK_START = '$';
const STATE = `${HOOK_START}state`;
const GET_VALUE = 'v';
const HYDRATE = 'h';
const CONDITIONAL = 'i';
const LIST = 'f'
const HOOK_REF = `${HOOK_START}ref`;

export {
  CONDITIONAL,
  LIST,
  CONFIG_ID,
  HOOK_START,
  HOOK_REF,
  STATE,
  GET_VALUE,
  HYDRATE,
}